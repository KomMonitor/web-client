# Analyse: Verbesserungspotential im Admin-Bereich

Stand: 2026-07-07. Analyse des gesamten Admin-Bereichs (`app/components/ngComponents/admin/**` und zugehörige Services) auf Code-Qualität, Duplikation und Architektur. Umfang: 208 Dateien — 89 `*.ts` (~20.900 Zeilen), 64 Templates (~16.000 Zeilen), 69 Komponenten.

**Kernbefund:** Das größte Verbesserungspotential liegt nicht in einer einzelnen Datei, sondern in einem Strukturproblem: Der komplette CRUD-Ablauf (Anlegen/Editieren/Löschen von Ressourcen) ist dreimal parallel implementiert — für Indikatoren, Georessourcen und Raumeinheiten — auf drei verschiedenen Reifegraden mit 60–80 % Duplikation in den Modals. Die Migration hat die AngularJS-Struktur Bildschirm für Bildschirm konserviert, statt sie zu vereinheitlichen. Die Zielarchitektur existiert bereits im Repo, verstreut über die drei Bereiche — sie muss zusammengeführt werden.

Ergänzt die bestehenden Dokumente `PROPOSED_CHANGES.md` und `documentation/PRIO7_GOD_SERVICE_SPLIT.md`; die CRUD-Verdreifachung und der `adminSpatialUnit`-Service-Zweig sind dort bisher unterrepräsentiert.

---

## 1. Die CRUD-Verdreifachung (größter Hebel)

Jeder der drei Bereiche (`adminIndicatorsManagement/`, `adminGeoresourcesManagement/`, `adminSpatialUnitsManagement/`) hat: Übersichtsseite (AG Grid) + Add-, Edit-Metadata-, Edit-Features-, Edit-User-Roles-, Delete- und Batch-Update-Modal.

### Drei Reifegrade

| Bereich | Architektur-Reife | Modal-Kommunikation | Importer-Anbindung | Feedback |
|---|---|---|---|---|
| **SpatialUnits** | modern (Referenz) | `@Output refreshRequested` | ausgelagerter `SpatialUnitImportService` + `spatial-unit-import.util.ts` | `NotificationService` |
| **Indicators** | modern, eigener Weg | `@Output refreshRequested` | inline `KommonitorImporterHelperService`, DOM-frei | `NotificationService` |
| **Georesources** | Legacy-Monolith | `BroadcastService` (37×), 0 EventEmitter | 33× `document.getElementById` | keine — inline Message-Strings |

Alle drei öffnen Modals modern via `NgbModal.open(...)` (kein jQuery-Modal-Handling mehr).

### Gemessene Duplikation

- **Add-Modals** (`georesource-add-modal.component.ts` 1260 Z. vs. `spatial-unit-add-modal.component.ts` 1277 Z.): **~65–70 % deckungsgleich**, bis hin zu wortgleich kopierten Kommentaren. Identisch: Metadaten-Objekt (`description/databasis/datasource/contact/updateInterval/lastUpdate/literature/note/sridEPSG`), Stepper-Navigation, Attribute-Mapping, Importer-Flow (dry-run `registerNew…(…, true)` → `importerResponseContainsErrors` → echter Call), Metadaten-Import/Export, ~90 Zeilen `resetForm`.
- **Edit-Metadata-Modals** (georesource 856 / spatial-unit 583 / indicator 792 Z.): zu **~70 % Teilmengen des jeweiligen Add-Modals** — Duplikation in zwei Dimensionen (zwischen Ressourcentypen *und* zwischen Add/Edit).
- **Edit-Features-Modals** (georesource 914 / spatial-unit 1032 / indicator 658 Z.): **~60 %** gemeinsamer Kern (Feature-Grid, Attribute-Mapping, Mapping-Config-Import/Export, dry-run-Ablauf).
- **Edit-User-Roles-Modals** (georesource 514 / spatial-unit 509 Z.): **~75–80 % identisch**.
- **Delete-Modals**: nur ~25–30 % — überwiegend echt ressourcenspezifisch (v. a. die aufwändige Teil-Lösch-Logik im `indicator-delete-modal`).

### Echt ressourcenspezifisch (nicht zusammenführen)

- POI/LOI/AOI-Styling + Icon-/Marker-Picker (Georesource, ~250 Z.).
- Outline-Layer, Raumebenen-Hierarchie (`nextLower/nextUpperHierarchyLevel`), Bbox (SpatialUnit, ~150 Z.).
- Indikator-Referenzen, Klassifikation/Colorbrewer, regionale Vergleichswerte, Precision/Unit, Teil-Lösch-Logik, pro-Raumebene-Rollen-Modal (Indicator).
- API-Vertragsunterschied `permissions` (SpatialUnit) vs. `allowedRoles` (Georesource) im PostBody — parametrisierbar halten.

### Konsolidierungsplan (Bausteine existieren bereits)

1. **Import-Flow generalisieren:** `SpatialUnitImportService` (271 Z.) + `spatial-unit-import.util.ts` + `spatial-unit-import.model.ts` sind bereits ressourcen-agnostisch geschrieben, werden aber nur von SpatialUnits genutzt. Zu einem generischen `ResourceImportService` machen, Georesource/Indicator umstellen. Einsparung grob 1.500–2.000 Zeilen; eliminiert nebenbei die `getElementById`-Zugriffe der Georesource-Modals.
2. **Gemeinsame `<resource-metadata-form>`-Komponente:** der identische Metadaten-Block (Formular + `parseFromMetadataFile` + Export/Import) steckt in 6 Modals.
3. **Gemeinsame `<role-management-panel>`-Komponente:** Rollen-Grid + Owner-Filter ist 4× neu implementiert (Add-, Edit-Metadata-, Edit-User-Roles-Modals, `indicator-add-form-state.service`), obwohl `RoleManagementDataGridHelperService` die Bausteine liefert.
4. **Stepper-/Validierungs-Basis:** `currentStep/nextStep/previousStep/goToStep` + Security-Step-Weiche ist in allen Add/Edit-Modals zeilenweise identisch.
5. **Georesource-Bereich mechanisch auf SpatialUnit-Muster heben** (Outputs statt Broadcast, `NotificationService`, DOM-Zugriffe raus) — Voraussetzung für 1–4.

### Sonderfall `indicator-add-form-state.service.ts` (2064 Z.)

Modal-scoped provided (frische Instanz pro Modal), teilt Formularstate zwischen den 7 Step-Komponenten (`steps/indicator-add-step1..7`) und bedient Add **und** Edit (`buildPostBody_indicators`, `buildPatchBody`, `enterEditMode`). Architektonisch das sauberste Muster im CRUD-Bereich — aber indikator-spezifisch; Metadaten-, Rollen-Grid-, Import/Export- und Stepper-Blöcke darin wären generisch extrahierbar.

---

## 2. Admin-Service-Schicht (zweitgrößter Hebel)

### `adminSpatialUnit/kommonitor-data-exchange.service.ts` (1192 Z.) — unerkannter God-Service mit irreführendem Namen

Fan-in: **16 Dateien**, darunter das gesamte Role-Management, `indicator-add-form-state.service` und sogar die Reachability-Coverage-Reports — faktisch ein globaler Admin/Auth-Service, kein SpatialUnit-Service. Mindestens 8 unzusammenhängende Verantwortlichkeiten:

- Keycloak-Auth-Polling per `timer(0, 1000)` (`:161`) + 30s-Token-Refresh-Polling (`:208`) + `setTimeout`-Retries (`:186-202`)
- HTTP-Fetch für SpatialUnits, AccessControl, Indikatoren (`:462`, `:494`, `:530` — `fetchIndicatorsMetadata` gehört hier gar nicht hin)
- eigener In-Memory-Cache mit 5-Min-TTL (`:96-111`, `:748-772`)
- State-Store mit 7 BehaviorSubjects (`:76-94`) plus synchrone `.value`-Getter — mehrere Wahrheitsquellen
- Permission-Logik (`checkCreatePermission :621`, `checkAdminPermission :827`, OU-Hierarchie-Traversierung `:855-887`)
- UI-Konstanten inkl. SVG-Strings (`datePickerOptions :372`, `availableLoiDashArrayObjects :423`)
- Formvalidierung + PATCH-Body-Bau (`:924-1054`)
- View-Logik: `syntaxHighlightJSON` erzeugt HTML-Strings (`:670`), `transformFeaturesForGrid` (`:1085`)

**Kontrast/Zielbild:** `adminGeoresourceUnit/kommonitor-data-exchange.service.ts` (134 Z.) ist eine saubere Fassade, die an `AccessControlService`, `GeoresourceMetadataStoreService`, `TopicMetadataStoreService`, `MetadataBootstrapService` und `CacheHelperService` delegiert — die Prio-7-Zielarchitektur. Der SpatialUnit-Zweig wurde beim Split schlicht nicht mitgezogen.

### Dreifach-Caching der Spatial-Unit-Metadaten

1. In-Memory-Cache im data-exchange (5-Min-TTL),
2. localStorage-Cache in `adminSpatialUnit/kommonitor-cache-helper.service.ts` (`:225-306`, mit eigenem, widersprüchlichem `SpatialUnitMetadata`-Interface),
3. zentraler `cache-helper-service`.

Zwei der Caches kennen sich nicht → Inkonsistenz eingebaut. `kommonitor-cache-helper.isUserAuthenticated()` gibt hart `true` zurück (`:118-122`). Die 22-Zeilen-Weiterleitungs-Services `adminIndicatorUnit/` und `adminGeoresourceUnit/kommonitor-cache-helper.service.ts` sind überflüssige Indirektion.

### Grid-Helper: HTML-String-Bau, DOM-Handler, tote Kopien

- **~400 Zeilen 1:1-Kopie** des Role-Management-Grids (drei `CheckboxRenderer_*`-Klassen + `buildRoleManagementGrid*`) in `adminFilterConfig/kommonitor-data-grid-helper.service.ts` (`:410-820`) — kanonisch existiert alles in `role-management-data-grid-helper-service`. Ebenso das `regionalReferenceValues`-Grid doppelt (adminIndicatorUnit `:361-572` und adminFilterConfig `:194-405`). Die echte Filter-Logik in adminFilterConfig umfasst nur ~70 Zeilen.
- Zellrenderer als HTML-Strings + `document.querySelectorAll(...).addEventListener` + `setTimeout(…, 200)`-Hacks (georesource-grid-helper `:41-66`, `:144-231`); Event-Routing über ID-String-Parsing (`event.target.id.split('_')[3]`, feature-table `split('__')`).
- **Tote AG-Grid-APIs:** `columnApi` und `new agGrid.Grid()` (in v31+ entfernt) in `adminFilterConfig` (`:125`, `:208`); globaler State auf `window.colState`/`window.filterState` (`:116-133`).
- POI/LOI/AOI-Column-Defs und Grid-Options im georesource-grid-helper dreifach nahezu identisch (`:300/:424/:541`, `:714/:752/:790`).

### `kommonitor-importer-helper.service.ts` (988 Z.)

Promise-basierter API-Client über **11× deprecated `.toPromise()`**; Fire-and-forget-Fetch im Konstruktor (`:258`); sequenzielle await-Schleifen statt `forkJoin`; **greift als Service per `document.getElementById` in Formularfelder** (`:449`, `:497-586` — der `formValues`-Fallback-Pfad existiert bereits und sollte der einzige werden); Management-API-Bodies durchweg `any`; Config aus `window.__env` statt `EnvConfigService`.

### Sicherheitsrelevanter Fund

`admin-login-state-service` hält `adminUserName`, `adminPassword`, `adminIsLoggedIn` als **öffentliche, untypisierte mutable Felder** (`:13-15`) — Klartext-Passwort im Singleton-State. Prüfen und entfernen.

### Weitere Service-Befunde

- `feature-table-data-grid-helper` (`providedIn: 'root'`) hält mutable Instanzfelder (`currentResourceId`, Grid-Referenzen) — zwei parallel geöffnete Edit-Modals überschreiben sich gegenseitig.
- Zwei divergierende HTTP-Error-Handler (`handleHttpError` liefert String, `handleError` wirft); `deleteSpatialUnit` verschluckt Fehlerursachen und liefert nur `boolean` (`:1145-1153`).
- `KeycloakHelperService` (1375 Z.) ist ein weiterer God-Service (Keycloak-Admin-REST komplett) — gleiches Split-Kandidatenprofil.

---

## 3. Querschnittsthemen

### Typisierung (Enabler für alles andere)

- **680 `: any`-Annotationen**, 59 `as any`, 125 `any[]`-Varianten im Admin-Code.
- **Null Interfaces für die zentralen API-Objekte** (Indicator-/Georesource-/SpatialUnit-Metadaten); nur 5 lokale `*.model.ts` für Randthemen. Selbst Broadcast-Payloads sind `any`.
- Empfehlung: Typen aus der OpenAPI-Spec der Data-Management-API generieren und sukzessive einziehen — ohne typisierte Modelle ist jedes größere Refactoring Blindflug.

### Formulare

**455 `ngModel`-Bindings, 0 Reactive Forms** im gesamten Admin-Bereich. Validierung über handgeschriebene `…Invalid`-Flags + `check…()`-Methoden, dreifach kopiert. Bei der CRUD-Konsolidierung direkt auf Typed Reactive Forms gehen, statt das ngModel-Muster zu verallgemeinern.

### AngularJS-Erbe in den Modal-Innereien

- **33 jQuery-Aufrufe** — konzentriert in den Edit-Features-/Edit-Metadata-Modals: `datepicker()`/`colorpicker()`/`iconpicker()`-Plugins (`georesource-edit-metadata-modal.component.ts:313-361`), bootstrap-validator (`indicator-edit-features-modal.component.ts:526`), AdminLTE `boxWidget()` (`admin-georesources-management.component.ts:208`).
- **59× `document.getElementById`** (Hotspot `georesource-edit-features-modal` ~15×), 72× `document.*` gesamt.
- **79 `setTimeout`-Timing-Hacks** (u. a. `admin.component.ts:49`, diverse Modal-Choreografien).
- **Broadcast-Bus** (176 Treffer) mit `Refresh…` → `…Completed`-Roundtrips als `$broadcast`-Ersatz.
- Async-Wildwuchs: 25× `.then(`, 84× `.subscribe(`, **7× deprecated `.toPromise()`**, 8× `firstValueFrom` — drei Paradigmen parallel.

### Feedback an den User: zwei konkurrierende Systeme

- Modern: `NotificationService` in 22 Dateien.
- Alt: `show/hideSuccessAlert`-Muster in 20 Dateien (154 Treffer) — statische Alert-Divs per `document.getElementById(...).hidden = false`, ohne Fehlertext aus der Response.
- Etliche Subscribes ohne Error-Handler; 49× `console.error`-only-Pfade.

### Change Detection / Rendering

- **0× OnPush**, Signals nur im Dashboard (`admin-dashboard-management.component.ts` — komplett signal-basiert, Vorbild) und 1× `signal(false)` im indicator-delete-modal.
- Riesige Templates mit Logik: `georesource-add-modal.component.html` 1487 Z. (44 `*ngIf`), `georesource-batch-update-modal.component.html` 1319 Z. (**80 `*ngIf`**), `spatial-unit-add-modal.component.html` 1172 Z. 491 `*ngIf` gesamt, kaum neue `@if`-Syntax.

### i18n

Nur 2 Admin-Dateien nutzen `translate`; **51 von 64 Templates** enthalten hartkodiertes Deutsch. Der Admin-Bereich ist faktisch nicht übersetzt, obwohl ngx-translate eingerichtet ist.

### `window.__env`

62 Direktzugriffe in 6 Dateien per `declare const __env: any` — am vorhandenen `EnvConfigService` vorbei (den 14 andere Admin-Dateien korrekt nutzen).

### Tests

**8 Specs für 89 TS-Dateien (~9 %)**, davon 7 reine "should create"-Smoke-Tests. Einzige echte Logiktests: `spatial-unit-import.util.spec.ts`. Die riskantesten Dateien (2064-Zeilen-Form-State, beide Add-Modals) sind ungetestet.

---

## 4. Shell, Routing & Config-Seiten

### Positiv

- Echtes Child-Routing: `AdminComponent` ist standalone mit `<router-outlet>`; 12 Child-Routes unter `administration` (`app/app.routes.ts:19-38`), nur der aktive Tab wird instanziiert.
- Qualitäts-Leuchttürme: Dashboard (Signals/`computed`), `advanced-role-permissions.ts` (sauber extrahiert, dokumentiert — bester Code im Admin-Bereich), `admin-script-execution` (sauberes `forkJoin`-RxJS), TopicsManagement (schlank, Store-basiert).

### Befunde

- **Kein Lazy Loading:** alle Admin-Routen statisch importiert (`component:` statt `loadComponent:`) — der komplette Admin-Bereich (~37.000 Zeilen TS+HTML) landet im Bundle jedes Nutzers, der nie `/administration` sieht. Billiger, großer Gewinn.
- **Voller Metadaten-Refetch bei jedem Betreten:** `AdminComponent.ngOnInit()` ruft `metadataBootstrap.fetchAllMetadata()` (`admin.component.ts:43`), obwohl der Startup bereits geladen hat; die `inFlight`-Serialisierung im Bootstrap-Service mildert nur Races, verhindert den Doppel-Fetch nicht. Dazu `setTimeout(prepUserInformation, 1000)`-Hack (`admin.component.ts:49-51`).
- **Tote Komponente:** `AdminLandingpageConfigComponent` ist vollständig implementiert, aber in keiner Route referenziert; enthält zudem einen wirkungslosen `await …subscribe()`-Bug (`:56`).
- **Config-Editoren** (AppConfig 302 Z. / ControlsConfig 321 Z.): Copy-Paste-Zwillinge — CodeMirror-Textareas mit "String-enthält-Keyword"-Validierung (50 hartkodierte `window.__env.*`-Keywords), `getElementById` statt des deklarierten `@ViewChild`, auskommentierte AngularJS-Downgrade-Reste. FilterConfig (519 Z.) ist moderner (`takeUntilDestroyed`), mischt aber CodeMirror + AG Grid + Broadcast.
- `AdminComponent` steht überflüssig im `imports`-Array von `AppModule` (`app.module.ts:92`), obwohl es nur per Route geladen wird.

---

## 5. Empfohlene Reihenfolge

> **Fortschritt (2026-07-07):** Schritt 1 (API-Typen) begonnen. OpenAPI-Spec der Data-Management-API unter `api-specs/` eingecheckt, Typ-Generierung per `npm run generate:api-types` (openapi-typescript) nach `app/models/data-management-api.generated.ts`, benannte Re-Exports in `app/models/data-management-api.ts`. Bestehende Hand-Modelle (`IndicatorsDataset`, `GeoresourcesDataset`, `Topic`) auf die Spec-Typen rebasiert; alle fünf Metadata-Stores (`indicator`/`georesource`/`spatial-unit`/`topic`/`process-script`) und der zentrale `cache-helper-service` (Fetch-Rückgaben) sind typisiert.
>
> **Fortschritt (2026-07-07, Teil 2):** `permissions.models.ts` auf `OrganizationalUnitOverviewType` rebasiert (`AccessControlPermission`/`AccessControlMetadata`/`AvailableRole`); `AccessControlService` komplett durchtypisiert. Die **beiden konkurrierenden Duplikat-Interfaces** `SpatialUnitMetadata`/`AccessControlMetadata` in `adminSpatialUnit/kommonitor-data-exchange.service.ts` und `adminSpatialUnit/kommonitor-cache-helper.service.ts` sind durch Aliasse auf die kanonischen Typen ersetzt (Legacy-Exportnamen bleiben für die 16 Importer bestehen). `admin-role-management.service` (`OrganizationalUnitInputType`-POST-Body) und `fetchIndicatorsMetadata` im Admin-Data-Exchange typisiert.
>
> **Fortschritt (2026-07-07, Teil 3):** `kommonitor-importer-helper`: alle 6 Register/Update-Bodies mit den Spec-Input-Typen (`SpatialUnitPOSTInputType` etc.) typisiert, `buildPutBody_indicators` typisiert (inkl. dokumentierter Spec-Abweichung `defaultClassificationMapping` im PUT-Body), 11× deprecated `.toPromise()` → `firstValueFrom`. Georesource-/Indicator-Grid-Helper und die Georesource-Fassade: Datenpfade typisiert (`GeoresourcesDataset[]`/`IndicatorsDataset[]`/`RegionalReferenceValueType[]`; Cell-Renderer-Params bewusst `any` gelassen — werden laut Roadmap durch Angular-Komponenten ersetzt). **`adminFilterConfig/kommonitor-data-grid-helper.service.ts` von 821 auf 92 Zeilen reduziert**: die toten 1:1-Kopien des Role-Management-Grids und des RegionalReferenceValues-Grids gelöscht (inkl. `window.colState`-Global-State und der in AG Grid v31 entfernten `columnApi`/`new agGrid.Grid()`-Aufrufe). Noch offen: restliche `: any` in den großen Add-/Edit-Modals; Aufräumkandidat: Debug-Fallback `testGridsWithSampleData()` in `admin-georesources-management.component.ts` (rendert bei Fetch-Fehlern Fake-Zeilen).
>
> **Fortschritt (2026-07-07, Teil 4 — Schritt 2 begonnen):** Georesource-Bereich aufs SpatialUnit-Muster gehoben. **Drei zuvor zur Laufzeit kaputte Modals repariert**: `georesource-delete-modal` und `georesource-edit-user-roles-modal` injizierten den AngularJS-String-Token `'kommonitorDataExchangeService'` ohne Provider (→ `NullInjectorError` beim Öffnen) — jetzt echte Services. Alle fünf Georesource-Modals (Add/Delete/EditMetadata/EditFeatures/EditUserRoles) kommunizieren jetzt via `@Output() refreshRequested` (`georesource-refresh.model.ts`) statt über den Broadcast-Bus; Delete-Daten laufen per `componentInstance`-Input statt `OnDeleteGeoresources`-Broadcast; der doppelte Refresh (Broadcast + `modalRef.result`) ist beseitigt; das Add-Modal sendete zudem falsche Payload-Keys (`action`/`id`), die immer den Full-Refetch-Fallback erzwangen. Der Broadcast-Listener der Übersicht bleibt nur noch für den externen Sender `wms-admin-table`. **Toter Event-Traffic entfernt:** `RefreshAdminDashboardDiagrams` hatte keinen Empfänger mehr (Dashboard ist signal-basiert) — alle 8 Sender + Enum-Member gelöscht; ebenso die Enum-Member `OnDeleteGeoresources`/`OnEditGeoresourceMetadata` und das tote Cell-Renderer-Callback-Trio der Übersicht. **Weiterhin kaputt (separates Thema):** `georesource-batch-update-modal` injiziert per String-Token drei Services, darunter den nicht mehr existierenden `kommonitorBatchUpdateHelperService` — das Modal crasht beim Öffnen und braucht eine echte Reparatur oder Entfernung (das Indicator-Pendant ist davon nicht betroffen).
>
> **Fortschritt (2026-07-07, Teil 5 — DOM/jQuery-Ablösung Georesource-Edit-Modals):** Wichtige Erkenntnis vorab: `angular.json` lädt nur jQuery-Core + Bootstrap — die jQuery-Plugins `datepicker`/`colorpicker`/`iconpicker` existieren zur Laufzeit **nicht**. Alle jQuery-Picker im `georesource-edit-metadata-modal` (Datum, LOI-/AOI-Farbe, POI-Icon, LOI-Linienmuster) waren daher seit der Migration stillschweigend tot (try/catch schluckte den TypeError). Ersetzt durch die vorhandenen Angular-`customElements`: `km-date-picker`, `km-color-picker`, `km-line-pattern-picker`; der tote POI-Icon-Picker-Button ist jetzt ein funktionierendes Text-Input für den Glyphicon-Namen (ein echter `km-icon-picker` wäre ein sinnvolles Folge-Feature, auch fürs Add-Modal). Im `georesource-edit-features-modal`: dynamische Converter-/Datasource-Parameter und BBOX-Felder werden nicht mehr per `getElementById` aus dem DOM gescraped, sondern sind ngModel-gebunden (`converterParameterValues`/`datasourceParameterValues`); Datei-Input über den vorhandenen `@ViewChild`. In beiden Modals: Alert-Ein-/Ausblenden über Template-Flags statt `document.getElementById(...).hidden`, `<pre>`-Beispielstruktur per `[innerHTML]`-Binding. Beide Modals sind jetzt frei von `document.*`- und jQuery-Zugriffen.
>
> **Fortschritt (2026-07-07, Teil 6 — Picker-Prüfung Add-Modal + Restbestände):** App-weite Prüfung auf tote jQuery-Picker-Aufrufe: Außerhalb des Admin-Bereichs nur noch auskommentierte Reste (Reachability, Data-Import). Im `georesource-add-modal` waren **alle Styling-Picker funktionsunfähig**: LOI-/AOI-Farbwahl (verstecktes Input ohne jegliche Picker-Init), POI-Icon-Button (nie initialisiert), Linienmuster-Dropdown und die drei POI-Dropdowns (Marker-Farbe/Art/Textfarbe) nutzten Bootstrap-3-Syntax (`data-toggle`) unter geladenem **Bootstrap 5** → Dropdowns konnten nicht öffnen. Fixes: Farben → `km-color-picker`, Datum → `km-date-picker`, Linienmuster → `km-line-pattern-picker`, Icon-Button → Text-Input; die POI-Dropdowns auf `data-bs-toggle` migriert und die `<a href="">`-Menüeinträge entschärft (hätten nach dem Fix Seiten-Reloads ausgelöst) — dieselben Dropdown-Fixes auch im Edit-Metadata-Modal. Im `georesource-edit-features-modal` die übersehene tote `initializeDatePickers()` entfernt und die Gültigkeitszeitraum-Inputs auf `km-date-picker` umgestellt (analog SpatialUnit-Vorlage). Verbleibende inerte `data-toggle="validator"`-Attribute (Plugin nicht geladen, folgenlos) in 6 Modals als Kosmetik-Rest notiert; `data-toggle="modal"` in `admin-filter-config.component.html:19` sollte bei nächster Gelegenheit geprüft werden.
>
> **Fortschritt (2026-07-07, Teil 7):** Tote `AdminLandingpageConfigComponent` (inkl. Template/SCSS) gelöscht — war in keiner Route referenziert, Selector nirgends verwendet. Aus dem Schritt-1-Paket damit noch offen: `adminPassword`-Klärung (Legacy-Login ohne Keycloak in `user-interface`/`user-login` — fachliche Entscheidung) und die restlichen `: any` in den großen Add-/Edit-Modals (gehen in Schritt 3 auf).
>
> **Fortschritt (2026-07-07, Teil 8 — Schritt 3 begonnen: `<app-resource-metadata-form>`):** Erster gemeinsamer Baustein extrahiert. Neu unter `adminShared/resourceMetadataForm/`: typisiertes Formularmodell (`buildResourceMetadataForm()` mit Typed Reactive Form, Pflichtfelder wie in den historischen Hand-Checks; `patchMetadataFormFromApi()` für Dataset-/Datei-Import inkl. `updateInterval`-apiName-Auflösung; `metadataFormToApi()` für POST/PATCH/Export-Bodies auf Basis `CommonMetadataType`) plus Standalone-Komponente mit dem kanonischen 8-Felder-Block — abgedeckt durch echte Logik-Tests (`resource-metadata-form.model.spec.ts`, 10 Tests). **Vier Modals umgestellt** (spatial-unit add/edit-metadata, georesource add/edit-metadata): der duplizierte Template-Block (~110 Zeilen × 4) ist durch die Komponente ersetzt, die handgestrickten `!metadata.x ||`-Button-Gates durch `metadataForm.invalid`, die kopierten Parse-/Reset-Blöcke durch die Modell-Utilities; die Modals behalten einen readonly `get metadata()`-Adapter für bestehende Body-/Export-Builder. Netto −584 Zeilen in den vier Modals. **Noch offen:** `indicator-add-form-state.service` (Step 2 des Wizards) und der Ausreißer `indicator-edit-metadata-modal` (abweichendes Layout, andere Pflichtfelder, `[value]`-Objekt-Bindings — separater Fix nötig) nutzen die Komponente noch nicht; als nächste Bausteine folgen `ResourceImportService` und `<role-management-panel>`.
>
> **Fortschritt (2026-07-07, Teil 9 — Metadaten-Form im Indikator-Wizard + `[ngValue]`-Bugfixes):** Der Indikator-Wizard nutzt jetzt ebenfalls `<app-resource-metadata-form>`: der `metadata`-State im `indicator-add-form-state.service` ist durch die typisierte FormGroup ersetzt (readonly-Getter-Adapter für die Body-Builder, Parse/Reset über die Modell-Utilities — erster Schnitt in den 2064-Zeilen-Service), Step 2 ist auf die gemeinsame Komponente umgestellt (das indikator-spezifische editierbare SRID-Feld bleibt als reactive gebundene Extra-Zeile). Damit ist der kanonische Metadaten-Block an 5 von 6 Stellen konsolidiert. **Echte Laufzeit-Bugs im `indicator-edit-metadata-modal` gefixt:** 9 Selects banden Objekte bzw. Zahlen per `[value]` statt `[ngValue]` — dadurch landete beim Auswählen der String `"[object Object]"` im Model: `updateInterval` ging als `undefined` ins PATCH, der `COMPUTATION`-Toggle des Erstellungstyps griff nie, niedrigste Raumeinheit/Themenhierarchie/Indikator- und Georessourcen-Referenzen waren nicht auswählbar. Alle auf `[ngValue]` (inkl. `null`-Platzhalter) umgestellt; `indicatorUnit` (String-Liste) bleibt korrekt bei `[value]`. **Gleiches Bug-Muster app-weit gefunden, noch offen:** `indicator-edit-features-modal` (u. a. `spatialUnit`, `converter`, `datasourceType`) und `reachability-indicator-statistics` (`spatialUnit`) — bei nächster Gelegenheit prüfen/fixen. Die Layout-Vereinheitlichung des Edit-Metadata-Modals (abweichende Pflichtfelder, flaches Layout) bleibt eine bewusste UX-Entscheidung für später.
>
> **Fortschritt (2026-07-08, Teil 10 — restliche `[ngValue]`-Bugfixes):** Die in Teil 9 notierten offenen `[value]`-Objekt-Bindings sind gefixt. `indicator-edit-features-modal`: 4 Selects (Raumebene der Zeitreihen-Übersicht, Quellformat/`converter`, Datenquelltyp/`datasourceType`, Ziel-Raumebene) auf `[ngValue]` mit `[ngValue]="null"`-Platzhaltern umgestellt; die Resets im TS setzen diese Modelle jetzt auf `null` statt `undefined`, damit die Platzhalter greifen. Konkret waren dadurch vorher kaputt: Zeitreihen-Übersicht laden/löschen (URL mit `undefined`-`spatialUnitId`), `onChangeConverter()` (Crash auf String), Schema-/Format-Folgeselects erschienen nie, `datasourceType?.type === 'FILE'`-Zweig griff nie, Rechte-Vorbelegung via `onChangeSelectedSpatialUnit()` lief ins Leere. `reachability-indicator-statistics`: Raumeinheit-Select ebenso auf `[ngValue]` (Statistik-Berechnung las `spatialUnitId` von einem String). String-Selects (`schema`, `mimeType`, `indicatorId`, `date`) bleiben korrekt bei `[value]`. Damit ist das app-weit gefundene Bug-Muster abgeräumt.
>
> **Fortschritt (2026-07-08, Teil 11 — Georesource-Batch-Update-Modal entfernt):** Die in Teil 4 offene Entscheidung Reparatur vs. Entfernung ist zugunsten **Entfernung** gefallen. Begründung: Das Modal crashte seit der Migration beim Öffnen (drei String-Token-Injections, darunter der nicht mehr existierende `kommonitorBatchUpdateHelperService`); das Indicator-Pendant ist selbst nur ein dokumentierter, bewusst funktionsloser WIP-Scaffold (`startBatchUpdate` No-op, leere Converter-/Datasource-Dropdowns); der Referenz-Bereich SpatialUnits hat kein Batch-Update — das Feature funktionierte also nirgendwo. Gelöscht: `georesourceBatchUpdateModal/` (Komponente + 1319-Zeilen-Template + SCSS), der Batch-Update-Button samt `onClickBatchUpdateGeoresource()` in der Übersicht, alle 8 `RefreshGeoresourceOverviewTableCompleted`-Broadcasts (einziger Konsument war das gelöschte Modal) inkl. Enum-Member sowie das untypisierte String-Literal-Event `'georesourceBatchListParsed'`. Die Enum-Member `BatchUpdateCompleted`/`ReopenBatchUpdateResultModal` bleiben — sie werden noch vom Indicator-Scaffold referenziert (dessen Kanäle allerdings ebenfalls ohne Gegenstelle sind; aufräumen, wenn über das Indicator-Batch-Update entschieden wird). Ein künftiges Batch-Update sollte als gemeinsamer Baustein für alle Ressourcentypen neu entstehen (Schritt-3-Muster), nicht pro Bereich. Damit ist der Broadcast-Bus in der Georesource-Übersicht auf den bewusst behaltenen `RefreshGeoresourceOverviewTable`-Listener (externer Sender `wms-admin-table`) reduziert; in den Modals verbleiben `AvailableRolesUpdate`/`OnEditGeoresourceFeatures`-Abos. **Aus Schritt 2 noch offen:** `NotificationService`-Umstellung der fünf Georesource-Modals (Inline-Alert-Flags), 2× `getElementById` beim Mapping-Config-Import im Add-Modal (`:841/:853`), `setTimeout`-Hacks (Hotspot Edit-User-Roles).
>
> **Fortschritt (2026-07-08, Teil 12 — Georesource: `getElementById` raus, `setTimeout`-Hacks abgebaut):** Das **Georesource-Add-Modal nutzt jetzt den `SpatialUnitImportService`** (erster Fremdnutzer des ressourcen-agnostischen Bausteins — Vorgriff auf den geplanten `ResourceImportService` aus Schritt 3): Converter-/Datasource-Parameter, Literal-BBOX und Datei laufen über ngModel-gebundene Records (`converterParameterValues`/`datasourceTypeParameterValues`, `bbox_*`-Felder, `@ViewChild`-File-Input) statt über den DOM-Scrape-Pfad des Importer-Helpers; die drei Builder-Wrapper sind durch einen `buildImporterObjects`-Aufruf ersetzt. Der Mapping-Config-Import schreibt in die Records statt per `getElementById` in DOM-Inputs — dabei nebenbei gefixt: Die importierten **BBOX-Einstellungen griffen vorher nie** (`element.value`-Schreibzugriffe auf ngModel-gebundene Selects sind wirkungslos, für die Literal-Koordinaten fehlten Model-Felder komplett); jetzt via `applyBbox()` analog SpatialUnit. Damit ist der Georesource-Bereich frei von echten `document.*`-Zugriffen. **`setTimeout`-Hacks entfernt** (9 Stück): Edit-Features (100ms vor Feature-Fetch, 2× 500ms Kunstverzögerung des Loading-Spinners), Edit-User-Roles (100ms im Dataset-Setter, 4× 250ms nach HTTP-Response, 1× leerer 250ms-Timeout), Delete (500ms). Die Alert-Auto-Hide-Timer (5s/10s) bleiben als legitime UX-Timer. **Bewusst offen gelassen:** die zwei `setTimeout`s der Übersichtsseite (`admin-georesources-management.component.ts:190/:229`) — sie hängen an den Interna des Georesource-Grid-Helpers (HTML-String-Renderer mit eigenem Timing) und gehören zu dessen separatem Umbau; und die `NotificationService`-Umstellung als letzter Schritt-2-Punkt.
>
> **Fortschritt (2026-07-08, Teil 13 — Schritt 2 abgeschlossen: `NotificationService` in allen Georesource-Modals):** Alle fünf Georesource-Modals melden Erfolg/Fehler jetzt per `NotificationService`-Toast nach dem Muster der SpatialUnit-Pendants; die Inline-Alert-Divs (`show/hideSuccessAlert`-Flags, `[innerHTML]`-Fehlerdumps via `syntaxHighlightJSON`, „Alert klebt unten im Modal") sind raus. Verhaltensangleichungen dabei: **Edit-Metadata und Edit-Features schließen sich nach erfolgreichem Update** (wie SpatialUnit), das **Delete-Modal schließt sich bei vollständigem Erfolg** und bleibt nur bei Teilfehlern offen (die Pro-Datensatz-Fehlertabelle bleibt inline); das Add-Modal toastet die Registrierung inkl. Feature-Anzahl (die alte Success-Alert war ohnehin nie sichtbar, weil das Modal direkt schloss). Bewusst inline geblieben (wie bei SpatialUnits): die Metadaten-/Mapping-Config-Import-Fehlerberichte mit JSON-Strukturbeispiel und die Per-Feature-Importer-Fehlerlisten (Add/Edit-Features). Edit-User-Roles toastet die zwei parallelen PUTs (Zugriffsrechte/Eigentümerschaft) getrennt mit spezifischen Meldungen; damit fielen auch die letzten Alert-Auto-Hide-Timer weg. Fehlertexte laufen über das gemeinsame `getErrorMessage()` aus `spatial-unit-import.util`. **Damit ist Schritt 2 („Georesource auf SpatialUnit-Niveau") abgeschlossen** — Rest: die zwei Grid-Helper-gebundenen `setTimeout`s der Übersicht (Teil des Grid-Helper-Umbaus). Nächster Schritt laut Plan: verbleibende Schritt-3-Bausteine (`ResourceImportService`-Umbenennung/Indicator-Umstellung, `<role-management-panel>`, Stepper-Basis) oder Schritt 4 (SpatialUnit-God-Service zerschlagen).
>
> **Fortschritt (2026-07-08, Teil 14 — Schritt 3: Role-Management-Panel extrahiert, erste zwei Modals umgestellt):** Neu unter `adminShared/roleManagementPanel/`: **`<app-role-management-grid>`** (Rollen-Grid mit optionalem „nur zugewiesene Rechte"-Toggle, `getSelectedRoleIds()`-API, `applyOwner()`-Reseeding, `reset()`), **`<app-owner-organization-select>`** (Eigentümerschafts-Übertragung: Stichwortfilter + Org-Select + Aktuell-Eigentümer-Spalte mit Warnung; Admins sehen alle Einheiten, sonst Creator-Rechte) und **`role-management-panel.model.ts`** (pure Funktionen `collectCreatorRightOrganizations` — mit dem Zyklus-Guard aus der SpatialUnit-Version, der der Georesource-Kopie fehlte —, `ownerDefaultPermissionIds`, `collectSelectedRoleIds`; 8 Logik-Tests). Zwei Shared-State-Fallen sind im Panel konstruktiv behoben: Es besitzt seine **eigene Grid-API** statt der einen geteilten auf dem root-provided `RoleManagementDataGridHelperService` (dessen `buildRoleManagementGrid` wird immer mit `null`-Options aufgerufen und berührt die geteilte API nie), und die `datasetOwner`-Flags werden auf einer **Kopie** der AccessControl-Liste gesetzt statt das geteilte Array zu mutieren — zwei gleichzeitig offene Grids können sich nicht mehr überschreiben. **Umgestellt: die beiden Edit-User-Roles-Modals** (SpatialUnit 509→~230 Zeilen TS, Georesource analog; Templates um je ~90 Zeilen dünner). Dabei den Georesource-Speicherablauf an das SpatialUnit-Muster angeglichen (sequenziell: erst Rechte, Ownership nur bei tatsächlicher Änderung, ein Erfolgs-Toast, Modal schließt) statt zwei paralleler PUTs; tote Reste entfernt (`onEditGeoresourcesUserRoles`-Listener ohne Sender, ungenutztes `declare __env`, Debug-`console.log`s, `.toPromise()`→`firstValueFrom` im SU-Modal). Wichtig für künftige Nutzer: Das Grid muss unter `[style.display]`-Step-Umschaltung stehen, nicht `@if` — der Checked-State lebt in der Komponente (das SU-Template wurde entsprechend von `@if` auf `display` umgestellt). **Noch auf das Panel umzustellen:** die Security-Steps der Add-Modals (SpatialUnit/Georesource, inkl. eigener Owner-Select-Variante ohne „nicht übertragen"), Rollen-Grids in den Edit-Metadata-Modals, `indicator-add-form-state.service`/Wizard-Step, `indicator-edit-indicator-spatial-unit-roles-modal` sowie die WMS- und Role-Management-Modals (insgesamt 10 weitere `RoleManagementDataGridHelperService`-Konsumenten).
>
> **Fortschritt (2026-07-08, Teil 15 — Role-Management-Panel in den Add-Modals):** Die Security-Steps der beiden Add-Modals (SpatialUnit/Georesource) nutzen jetzt das Panel. `<app-owner-organization-select>` hat dafür einen **`mode="assign"`** bekommen (Pflichtauswahl der Eigentümer-Organisationseinheit mit Info-Alert und Platzhalter-Option, ohne „nicht übertragen"/Aktuell-Eigentümer-Spalte), das Grid eine **`applyPermissions()`**-API für den Metadaten-Datei-Import. Beide Security-Fieldsets sind von `@if`-Step-Gating auf `[style.display]` umgestellt, damit der Checkbox-State Step-Wechsel überlebt. **Zwei tote/kaputte Stellen dabei repariert:** Im Georesource-Add-Modal wurde das Rollen-Grid seit der Migration **nie gerendert** (das Template enthielt nur ein leeres `div` statt `<ag-grid-angular>` — Grid-Options wurden ins Leere gebaut, `allowedRoles` blieb bis auf den Datei-Import-Pfad immer leer) und die Owner-Auswahl seedete keine Default-Rechte, obwohl der Hinweistext das behauptete — beides funktioniert jetzt wie im SpatialUnit-Vorbild (Owner-Wechsel checkt Viewer/Editor der Eigentümer-Einheit vor). Im SpatialUnit-Add-Modal fielen zusätzlich die Header-Height-DOM-Hacks (`querySelector` + `setGridApi`-Kopplung an den Shared-Helper) und die vereinfachte `prepareCreatorList`-Kopie (ohne Kind-Expansion) weg. Netto −343 Zeilen. **Verbleibende Panel-Konsumenten:** Edit-Metadata-Rollen-Grids, `indicator-add-form-state.service`/Wizard-Step, `indicator-edit-indicator-spatial-unit-roles-modal`, WMS- und Role-Management-Modals.
>
> **Fortschritt (2026-07-08, Teil 16 — Panel im Indikator-Wizard; unsichtbares Grid im Georesource-Edit-Metadata entfernt):** Zwei weitere Konsumenten abgeräumt. **Georesource-Edit-Metadata:** Auch hier wurde das Rollen-Grid nie gerendert (kein `<ag-grid-angular>` im Template); es diente nur als Vehikel, die `allowedRoles` des Datensatzes (bzw. der importierten Datei) in PATCH-Body und Export durchzureichen. Ersetzt durch ein schlichtes `allowedRoles`-Feld mit identischer Semantik — das Modal verwaltet wie das SpatialUnit-Pendant keine Rechte mehr (Job des Edit-User-Roles-Modals); Grid-Helper-Abhängigkeit, `AvailableRolesUpdate`-Abo und Broadcast-Plumbing entfielen. **Indikator-Wizard (Step 7):** Das Rollen-Grid ist jetzt das Shared-Panel. Weil die Step-Komponenten bei der Navigation zerstört werden, gibt es einen Attach/Detach-Handshake mit dem `indicator-add-form-state.service`: Step 7 registriert sein Grid beim View-Init (Seeding aus geharvesteter Auswahl bzw. Owner-/Edit-Mode-Defaults) und harvestet die Auswahl beim Destroy in `storedPermissionIds`; `getSelectedRoleIds()` im Service liest bevorzugt das lebende Grid (Submit auf Step 7 möglich). Damit flogen aus dem Form-State-Service die komplette Grid-Konfiguration, die Grid-Event-Handler, die Header-Height-DOM-Hacks und die `setGridApi`-Kopplung an den Shared-Helper raus (zweiter Schnitt in den 2000-Zeilen-Service). Grid-Block im Step-7-Template auf `[style.display]` umgestellt (Handshake braucht die existierende Komponente). **Verbleibende `RoleManagementDataGridHelperService`-Direktnutzer:** `indicator-edit-indicator-spatial-unit-roles-modal`, `indicator-edit-features-modal`, die WMS-Modals (add/edit-user-roles) und die Role-Management-Modals (role-add, role-edit-group-rights) — letztere nutzen das Grid mit anderen Spalten/Semantik und sind ggf. kein Panel-Fall.
>
> **Fortschritt (2026-07-08, Teil 17 — Indikator-Rollen-Modals: zwei weitere unsichtbare Grids repariert bzw. entfernt):** **`indicator-edit-indicator-spatial-unit-roles-modal` grundlegend repariert:** Beide Rollen-Grids (Metadaten-Rechte in Step 1, Zeitreihen-Rechte pro Raumebene in Step 2) waren seit der Migration **nie sichtbar** (leere `div`s statt `<ag-grid-angular>` — die PUTs schickten nur die geseedeten Werte zurück), und das Ziel-Raumebenen-Select in Step 2 hatte den `[value]`-Objekt-Bug aus Teil 9/10 (Model bekam `"[object Object]"` → `permissions`/`isPublic` der Raumebene liefen ins Leere). Jetzt: zwei `<app-role-management-grid>`-Instanzen (die Eigene-Grid-API-Architektur des Panels macht zwei gleichzeitige Grids erst korrekt möglich), `[ngValue]`-Fix, Owner-Übertragung über `<app-owner-organization-select>`, Fieldsets display-basiert. Datenquelle wechselt dabei von `AccessControlService` auf den `KommonitorDataExchangeService` des Panels (im Admin-Indikator-Bereich die verlässlich befüllte Quelle, vgl. Kommentar im Form-State-Service). Die vier parallelen PUT-Requests (Metadaten-Rechte, Ownership, Zeitreihen-Rechte, Zeitreihen-Ownership je Raumebene) blieben mechanisch erhalten — die Sequenzialisierung nach SU-Muster wäre ein Folgeschritt. **`indicator-edit-features-modal`:** Das dritte unsichtbare Grid entfernt — es diente nur als Durchreiche (Zeitreihen-Permissions der Ziel-Raumebene + Owner-Defaults) in den Importer-PUT-Body und den Mapping-Config-Export; ersetzt durch `currentRolePermissionIds()` auf Basis der puren `ownerDefaultPermissionIds()` aus dem Panel-Modell, identische Semantik inkl. Dedup. **Zwischenstand Panel-Konsolidierung:** 7 von 12 Konsumenten umgestellt, 3 unsichtbare/tote Grid-Instanzen ersatzlos bzw. durch Passthroughs ersetzt. **Bewusst offen:** die WMS-Modals (echte, gerenderte Grids, `AccessControlService`-Quelle, hängen teils außerhalb des Admin-Bereichs — Umstellung wäre ein Datenquellen-Wechsel und braucht eine eigene Prüfung) und die Role-Management-Modals (andere Spalten/Semantik, vermutlich kein Panel-Fall).
>
> **Fortschritt (2026-07-08, Teil 18 — Panel-Konsolidierung abgeschlossen: WMS-Modals + Helper-Entschlackung):** Die Prüfung ergab: `wms-admin-table` wird **nur** in den Admin-Seiten (Georesources/Indicators) eingebettet — der Datenquellen-Wechsel auf den vom Panel genutzten `KommonitorDataExchangeService` ist damit unkritisch (der `AccessControlService` wird ohnehin vom selben `MetadataBootstrapService` beim Admin-Einstieg befüllt). **Beide WMS-Modals aufs Panel umgestellt.** Dabei behobene Bugs: `addWms()`/`editData()` lasen die Rollen-Selektion aus den **GridOptions** (die gar kein `rowData` haben) statt den TableOptions und funktionierten nur zufällig über die geteilte Helper-Grid-API — sprich: war zuletzt ein *anderes* Rollen-Grid registriert, wurden **dessen** Häkchen gespeichert. Im `wms-add-modal` sahen Nicht-Admins zudem gar keine Owner-Auswahl (fehlender Nicht-Admin-Zweig → Registrieren-Button blieb für sie dauerhaft deaktiviert) — der `assign`-Modus des Owner-Selects zeigt ihnen jetzt ihre Creator-Rechte-Einheiten. Das `wms-edit-user-roles-modal` bekam statt des Assign-Texts („Datensatz anlegen") die reguläre Transfer-Ansicht mit Aktuell-Eigentümer-Anzeige; das bewusste Nicht-Reseeding bei Ownerwechsel (auskommentiertes Legacy-`refreshRoles`) ist als Kommentar dokumentiert. **`RoleManagementDataGridHelperService` entschlackt** (606→~500 Zeilen): die geteilte `gridApi` samt `setGridApi`/`getSelectedRoleIds_roleManagementGrid` und dem Update-Branch von `buildRoleManagementGrid` ist komplett entfernt — das in der Analyse notierte „zwei offene Grids überschreiben sich"-Risiko existiert nicht mehr; der Helper liefert nur noch Options-/ColDef-Bausteine und die Checkbox-Renderer. Verbleibende Helper-Nutzer: das Panel und die zwei Role-Management-Modals (Custom-Grids, nutzen nur die Basis-Builder). **Damit ist der Schritt-3-Baustein `<role-management-panel>` vollständig ausgerollt** — alle 9 Ressourcen-Rechte-Grids laufen über das Panel bzw. schlichte Passthroughs. Nächste Schritt-3-Bausteine: `ResourceImportService`-Generalisierung (Indicator-Umstellung) und die Stepper-/Validierungs-Basis; danach Schritt 4 (SpatialUnit-God-Service).
>
> **Fortschritt (2026-07-08, Teil 19 — `ResourceImportService`: Generalisierung abgeschlossen, DOM-Scrape-Pfad eliminiert):** Der `SpatialUnitImportService` heißt jetzt **`ResourceImportService`** und lebt mit seinem Modell unter `services/resource-import-service/` (Konsumenten-Importe aktualisiert; `spatial-unit-import.util.ts` mit den SU-spezifischen Helfern + `getErrorMessage` bleibt vorerst am alten Ort). Neu: **granulare öffentliche Builder** `buildConverterDefinition(config)` / `buildDatasourceTypeDefinition(config)` für Nicht-Spatial-Konsumenten. **`indicator-edit-features-modal` umgestellt** — der letzte lebende DOM-Scrape-Pfad: Converter-/Datasource-Parameter sind jetzt ngModel-Records, die Datei kommt per `@ViewChild`, und die toten `$('#…').validator(...)`-Aufrufe im Validierungs-Fehlerpfad (Plugin nicht geladen → warf TypeError) sind durch einen Fehler-Toast ersetzt; die unerreichbaren CRS-Input-Zweige im Template (der äußere `@if` versteckt CRS-Parameter komplett) sind entfernt — CRS wird bewusst weiterhin nicht gesendet (historisches Verhalten). **Damit sind die `document.getElementById`-Fallback-Zweige im `kommonitor-importer-helper` toter Code geworden und wurden entfernt** (der Roadmap-Punkt „der formValues-Fallback-Pfad sollte der einzige werden" ist erledigt): `buildConverterDefinition`/`buildDatasourceTypeDefinition` nehmen nur noch formValues, die Präfix-/FileInputId-Parameter sind aus Signaturen, `ImporterObjectsConfig` und allen vier Modal-Configs entfernt; der FILE-Zweig (Upload) liegt allein beim `ResourceImportService`. **Georesource-Add-Modal:** nutzt jetzt `collectMissingImporterFields()` — statt des historischen stillen Abbruchs bei unvollständigem Import-Formular werden die fehlenden Pflichtfelder als Toast benannt. **Bewusst nicht umgestellt:** `georesource-edit-features-modal` behält seinen direkten Data-API-PUT (`/georesources/{id}/features` mit eigenem Body-Format, Parameter-Maps statt -Arrays) statt des Importer-Dry-Run-Flows der SU-/Indicator-Pendants — ein Wechsel des Backend-Pfads wäre eine funktionale Änderung, keine Refaktorierung, und braucht eine eigene fachliche Entscheidung + Backend-Test. Aus Schritt 3 verbleibt die Stepper-/Validierungs-Basis.
>
> **Fortschritt (2026-07-08, Teil 20 — Stepper-Basis: `WizardStepper` extrahiert, die vier Security-Wizards umgestellt):** Neu unter `common/stepper/wizard-stepper.ts`: die Klasse **`WizardStepper`** kapselt den Step-Flow der Wizard-Modals (deklarative Step-Definitionen mit `key`/`label`/optionalem `when()`-Sichtbarkeits-Prädikat; `next`/`previous`/`goTo`/`reset`; stabile `steps`-Referenz für den OnPush-`<app-stepper>`; 4 Logik-Tests). Der Gewinn gegenüber den Kopien: Templates adressieren Steps **per Key** (`stepper.isActive('security')`) statt per Index-Arithmetik — die fehleranfälligen Doppel-Bedingungen wie `(currentStep === 4 && !security) || (currentStep === 5 && security)` und die duplizierten with/without-Security-Label-Arrays entfallen. **Umgestellt: die vier Wizards mit bedingtem Security-Step** — SpatialUnit-Add, Georesource-Add, Indikator-Wizard (`indicator-add-form-state.service`; `nextStep`/`previousStep`/`goToStep` bleiben dort als dünne Delegates, weil die 7 Step-Templates sie binden) und WMS-Add. Dabei behoben: Im WMS-Add-Modal war `totalSteps` hart 4 — ohne Keycloak konnte man auf einen leeren vierten Step navigieren; der Security-Step verschwindet jetzt aus der Navigation. Tote `initializeMultiStepForm`-`totalSteps`-Zuweisungen (SU-Add zählte sogar auf 5/4 statt 4/3) sind mit raus. **Noch offen (mechanisch, ohne bedingte Steps):** die zehn Fixed-Step-Modals (Edit-Metadata/Edit-Features/Edit-User-Roles × SU/Geo, Indicator-Edit-Features, Indicator-SpatialUnit-Roles, WMS-Edit(-User-Roles), Script-Add, Admin-Filter-Edit) tragen weiterhin die kopierten `nextStep`/`previousStep`-Trios — Umstellung ist rein mechanisch und kann sukzessive erfolgen. Damit sind die drei großen Schritt-3-Bausteine (Metadaten-Form, Role-Panel, Import-Service) plus Stepper-Kern erledigt; nächster großer Block laut Plan: **Schritt 4** (SpatialUnit-God-Service zerschlagen).
>
> **Fortschritt (2026-07-08, Teil 21 — Stepper-Basis flächendeckend: alle 14 Fixed-Step-Modals umgestellt):** Die in Teil 20 notierten restlichen Modals nutzen jetzt ebenfalls den `WizardStepper`: Edit-Metadata/Edit-Features/Edit-User-Roles in beiden Bereichen (SU/Geo), die beiden Indicator-Edit-Modals, WMS-Edit und WMS-Edit-User-Roles, Script-Add, Admin-Filter-Edit sowie die zwei Role-Management-Modals (role-add, role-edit-group-rights — die navigieren rein über Stepper-Klicks, kein next/previous). Templates adressieren Steps durchgängig per Key; die kopierten `currentStep`/`totalSteps`-Felder, `steps`-Arrays und `nextStep`/`previousStep`/`goToStep`-Trios sind aus allen Komponenten entfernt (einzige verbleibende Delegates: `indicator-add-form-state.service`, dessen 7 Step-Templates sie binden). **Damit ist Schritt 3 („Gemeinsame Bausteine extrahieren") vollständig abgeschlossen** — `<resource-metadata-form>`, `<role-management-panel>`, `ResourceImportService` und `WizardStepper` sind ausgerollt. Nächster Block: **Schritt 4** (SpatialUnit-God-Service) oder Schritt 5 (Lazy Loading, Doppel-Fetch, Signals/OnPush, i18n).
>
> **Fortschritt (2026-07-08, Teil 22 — Schritt 4: God-Service zur Fassade, Dreifach-Cache konsolidiert):** **`adminSpatialUnit/KommonitorDataExchangeService` ist von 1156 auf ~270 Zeilen geschrumpft** — jetzt eine dünne Fassade nach dem Muster des Georesource-Pendants. Entfernt (alles Interna ohne externe Konsumenten, per Nutzungsmatrix über die 20 Importer verifiziert): das **duplizierte Keycloak-Polling** (1s-Timer + 500ms-Retries + 30s-Refresh — Rollen kommen jetzt aus `AccessControlService.applyLoginStateFromToken`, das der Bootstrap befüllt), der **private 5-Min-In-Memory-Cache**, die eigenen HTTP-Fetches und 5 der 7 BehaviorSubjects. Delegation: Rollen/Permissions/AccessControl → `AccessControlService`; SpatialUnit-Kollektion → `SpatialUnitMetadataStoreService` (um `addSingle`/`replaceSingle`/`deleteSingle` + `availableSpatialUnits$` via `toObservable` erweitert); Fetches → `MetadataBootstrapService`; URLs/Config → `EnvConfigService`/`CacheHelperService`; `syntaxHighlightJSON`/`formatErrorMessage` → `IndicatorValueService`; Body-Bau/Validierung/Grid-Transforms → neues pures **`spatial-unit-metadata.util.ts`**. `loading$`/`error$` bleiben als kleine, von den Fassaden-Fetches gespeiste Subjects (die SU-Übersicht bindet sie). **Dabei behobene Bugs:** `fetchIndicatorsMetadata` gab ein Observable zurück, das der einzige Aufrufer (SU-Delete-Modal) awaitete ohne zu subscriben — der Refetch lief nie (jetzt Promise); `deleteSpatialUnit` nutzt `firstValueFrom` statt `.toPromise()`. **Dreifach-Cache aufgelöst:** der 396-Zeilen-`adminSpatialUnit/kommonitor-cache-helper` (localStorage-Zweitcache, `isUserAuthenticated()` hart `true`) und die zwei 22-Zeilen-Weiterleitungs-Services (`adminIndicatorUnit`/`adminGeoresourceUnit`) sind **gelöscht**; die drei Overview-Seiten nutzen den zentralen `cache-helper-service` direkt. Dessen sieben **`fetchSingle*`-Methoden waren alle kaputt**: Sie gaben die `.subscribe()`-Subscription statt der Daten zurück — die Overviews schrieben also `Subscription`-Objekte in ihre Stores (der gezielte Einzel-Refresh nach Add/Edit hat nie funktioniert; gerettet nur dadurch, dass parallel ein versteckter Full-Refetch lief). Jetzt `async`/`firstValueFrom` mit echten Rückgabetypen, ohne den versteckten Full-Refetch. Netto −889 Zeilen. **Aus Schritt 4 verbleibt:** die Umbenennung der Fassade (Name `KommonitorDataExchangeService` kollidiert konzeptionell mit dem zentralen data-exchange-service; 20 Importer), das sukzessive Umstellen der Konsumenten direkt auf die kanonischen Services sowie der `KeycloakHelperService` (1375 Z.) als zweiter God-Service-Kandidat.
>
> **Fortschritt (2026-07-08, Teil 23 — Fassade umbenannt, toter Reachability-Helper entfernt):** Die Fassade heißt jetzt **`KommonitorSpatialUnitDataExchangeService`** (analog `KommonitorGeoresourceDataExchangeService`; alle 19 Importer per Klassen-Rename umgestellt, Property-Namen `kommonitorDataExchangeService` in den Komponenten/Templates bewusst unangetastet). Die Namenskollision mit dem zentralen data-exchange-service ist damit beseitigt. **Nebenfund:** Der in der Analyse als God-Service-Konsument gezählte `kommonitorReachabilityCoverageReportsHelperService` (`app/util/genericServices/`) war **toter Code** — null Referenzen und ein Import auf den nicht existierenden Pfad `util/data-exchange/`; er überlebte nur, weil der esbuild-Builder unreferenzierte Dateien nicht kompiliert. Gelöscht (−700 Z.). Im selben Ordner liegt noch ein nicht geladenes AngularJS-`.js`-Modul (`kommonitorReachabilityScenarioHelperService`) — Legacy-Cleanup gemäß `PROPOSED_CHANGES.md`, hier nicht angefasst. **Aus Schritt 4 verbleibt:** die sukzessive Direktumstellung der 19 Fassaden-Konsumenten auf die kanonischen Services (mechanisch, pro Konsument klein) und der `KeycloakHelperService` (1375 Z.) als zweiter God-Service-Kandidat.
>
> **Fortschritt (2026-07-08, Teil 24 — Schritt 4 abgeschlossen: Fassade gelöscht):** Alle 19 Konsumenten der SpatialUnit-Fassade sind direkt auf die kanonischen Services umgestellt; **die Fassade selbst ist gelöscht** — der ehemalige 1156-Zeilen-God-Service existiert nicht mehr. Umstellungs-Mapping: Rollen/AccessControl/Permissions → `AccessControlService`; SpatialUnit-Kollektion + Mutationen + Stream → `SpatialUnitMetadataStoreService`; Fetches → `MetadataBootstrapService` (Promise-basiert, die Observable-Wrapper der Fassade entfielen); URLs/`enableKeycloakSecurity`/`updateIntervalOptions` → `EnvConfigService`; spatialResource-URL → `CacheHelperService`; `syntaxHighlightJSON`/`formatError` → `IndicatorValueService`; Body-Bau/Validierung/Grid-Transforms/LOI-Presets → Direkt-Importe aus `spatial-unit-metadata.util.ts`; `SpatialUnitMetadata`-Typ-Importe → `SpatialUnitOverviewType` aus den generierten API-Modellen, `AccessControlMetadata` → `permissions.models`. `bulkDeleteSpatialUnits` (einziger Konsument: SU-Delete-Modal) ist in das Modal gewandert. Die `loading$`/`error$`-Subjects der Fassade entfielen — die SU-Übersicht verwaltet ihren Loading-State lokal um die Bootstrap-Promises und meldet Fehler direkt per Toast. Der Ordner `adminSpatialUnit/` enthält nur noch Grid-Helper, Importer-Helper und das Metadata-Util. **Damit ist Schritt 4 (SpatialUnit-God-Service) vollständig abgeschlossen**; als God-Service-Kandidat verbleibt der `KeycloakHelperService` (1375 Z.), danach Schritt 5 (Lazy Loading als billigster großer Gewinn, Doppel-Fetch, Signals/OnPush, i18n, `__env`-Zugriffe).
>
> **Fortschritt (2026-07-08, Teil 25 — `KeycloakHelperService` neu geschrieben: 1375→269 Zeilen):** Die Analyse des zweiten God-Service ergab zwei strukturelle Befunde. **(1) Der `return await ….subscribe()`-Bug aus dem Cache-Helper steckte in allen ~30 HTTP-Methoden**: Awaits lösten sofort auf (Admin-Requests liefen als Race statt sequenziell), Rückgaben waren Subscriptions statt Payloads (der Rollen-Cache hielt eine Subscription — Rollen-Umbenennen/-Löschen warf beim Iterieren), und Fehler erreichten die Aufrufer nie. **(2) ~700 Zeilen Keycloak-Gruppen-Update/-Delete und Fine-Grained-Permission-Policies hatten null Aufrufer** in der App (per Grep über alle 14 Methodennamen verifiziert) — und hätten wegen (1) ohnehin nicht funktionieren können. Der Service ist neu geschrieben (269 Z.): nur die lebende Oberfläche der drei Konsumenten (`init`, `fetchAndSetKeycloakRoles`, `renameExistingRoles`, `deleteRoles`, `postNewGroup`, `targetRealmUrlToKeycloakInstance`), `firstValueFrom`-basiert mit echter Sequenzierung, Fehler-Propagation, typisiertem Rollen-Cache und klaren Fehlermeldungen bei fehlenden Rollen; gemeinsames URL-/Auth-Header-Plumbing statt 30 Kopien. Der tote Block ist gelöscht (Git-Historie als Ausgangspunkt für eine Wiederbelebung). **Dabei explizit gewordene fachliche Lücke:** Beim Löschen/Umbenennen einer Organisationseinheit im Admin-UI werden nur die Realm-Rollen gelöscht/umbenannt — die **Keycloak-Gruppe bleibt unangetastet** (war schon immer so; der Code dafür existierte, war aber nie angebunden und kaputt). Neue Spec mit 7 echten Tests (Sequenzierung, Body-Aufbau, Fehlerpfade) statt des Smoke-Tests. Lint-Warnings sanken durch den Rewrite von 1448 auf 1377. **Damit sind beide God-Services der Analyse abgearbeitet** — es verbleibt Schritt 5.

1. **API-Typen einführen** (Enabler, risikoarm) + tote Kopien löschen (Role-/ReferenceValues-Grid-Kopien in `adminFilterConfig`, `AdminLandingpageConfigComponent`) + `adminPassword`-Feld im `admin-login-state-service` klären/entfernen.
2. **Georesource-Bereich auf SpatialUnit-Niveau heben:** Broadcast → `@Output`, `getElementById` raus, `NotificationService` — rein mechanisch, macht die drei Bereiche verhaltensgleich.
3. **Gemeinsame Bausteine extrahieren:** `ResourceImportService` (aus `SpatialUnitImportService` generalisiert), `<resource-metadata-form>`, `<role-management-panel>`, Stepper-Basis — der große Zeilen-Abbau (grob 4.000–6.000 Zeilen über alle Modals). Neue Bausteine mit Typed Reactive Forms und echten Tests.
4. **`adminSpatialUnit/KommonitorDataExchangeService` zerschlagen** nach dem Muster der Georesource-Fassade: Auth/Polling → Auth-/AccessControl-Services, UI-Konstanten/Body-Bau/Validierung in dedizierte Module, Caches auf den zentralen `cache-helper-service` konsolidieren, danach umbenennen.
5. **Modernisierung in der Breite:** Lazy Loading der Admin-Routen, Doppel-Fetch beim Admin-Einstieg beseitigen, Signals/OnPush ausrollen (Vorbild Dashboard), `.toPromise()`/`setTimeout`-Hacks entfernen, Feedback auf `NotificationService` vereinheitlichen, i18n der 51 Templates, `__env`-Zugriffe auf `EnvConfigService`.
